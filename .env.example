# 로컬 개발용 docker-compose.yml(MySQL) + 백엔드 로컬 실행에서 사용하는 값.
# 이 파일을 .env 로 복사해서 쓴다. .env 는 절대 커밋하지 않는다.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=samsam55
DB_USERNAME=samsam55
DB_PASSWORD=samsam55
DB_ROOT_PASSWORD=root
